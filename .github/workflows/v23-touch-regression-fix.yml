name: V23 Touch Regression Repair

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  already-applied:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Touch regression repair is already applied on v23-upload-ready-nightly."
